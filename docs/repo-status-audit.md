# FutureLight Repo Status Audit

Created: 2026-06-01 10:33:37 +08:00
Backlog: P0-BLOCKER-003

This audit records what is real in the repository right now, what is still fixed/demo data, and what cannot be marked complete under the no-mock delivery rule.

## Verification Snapshot

Commands run on 2026-06-01 10:33 +08:00:

```text
cargo check
npm.cmd run build
python json validation for content/courses/animal-english-words.json and assets/asset_manifest.json
rg -n "mock|placeholder|TODO|planned|fallback|hardcoded|demo|draft|null" -S . -g "!node_modules" -g "!target" -g "!dist" -g "!*.lock"
```

Results:

| Check | Result | Notes |
| --- | --- | --- |
| Rust compile | Pass | `backend` builds with the current Axum health/demo handlers and migration runner. |
| Frontend build | Pass | React/Vite production build succeeds. |
| Course JSON parse | Pass | `content/courses/animal-english-words.json` is valid JSON. |
| Asset manifest JSON parse | Pass | `assets/asset_manifest.json` is valid JSON. |
| No-mock scan | Findings | Static/fallback/draft/planned items remain and are listed below. |

## Component Status

| Area | Real Today | Not Yet Real | Completion Status |
| --- | --- | --- | --- |
| React frontend | Vite app builds; frontend has URL-driven routes for `/`, `/courses`, `/learn`, `/practice`, `/parent`, `/content`, `/settings`, plus a not-found state; History API navigation and `popstate` browser back handling are implemented; `/parent` calls real auth APIs for register, login, session restore, settings update, sound preference update, and logout with token storage and error states; signed-in parents can list, create, edit, and delete child profiles through `/api/children`; onboarding captures age band, market region, English variant, and optional parent privacy consent through `/api/privacy/consents`; parent center shows per-child progress/reward summaries, consent records with revoke actions, downloadable data export package generation plus recent export request status rows, privacy child data deletion, parent account deletion, and DB-backed sound controls; settings save, data export, consent revoke, privacy child deletion, parent account deletion, and child profile deletion require a password-backed Parent Gate verified through `/api/auth/login`; `/content` exposes the first content-admin workflow for admin-gated course, lesson, activity, asset selection, publish check, and version history operations; SoundProvider plays UI/practice/learning cues from real WAV files and honors saved effect-volume/sound-enabled settings; `/courses` reads courses, lessons, objectives, and activities from the course APIs; `/learn` starts sessions, records attempts, refreshes progress/rewards, and completes sessions through the learning APIs while passing the selected child's market/English variant to content APIs; `/practice` grades single-choice activity choices against `answer_key`, records attempts, refreshes progress/rewards with reward pills, and also passes the selected child's market/English variant; frontend maps `parental_consent_required` backend errors for missing US/DE/UK consent; frontend fetches `/health` and `/api/home/summary`; cover image and UI audio are served from `frontend/public/assets`. Chrome headless, Playwright CLI route/back smoke, Playwright CLI auth smoke, Playwright CLI child CRUD smoke, Playwright CLI course smoke, Playwright CLI learning smoke, Playwright CLI practice smoke, Playwright CLI reward smoke, Playwright CLI parent-center smoke, Playwright CLI parent-gate smoke, Playwright CLI sound-settings smoke, Playwright CLI content-admin smoke, Browser visual smoke desktop/mobile route checks, PostgreSQL consent checks, PostgreSQL progress checks, PostgreSQL reward checks, PostgreSQL content-admin CRUD/version checks, PostgreSQL parent/audit/settings checks passed, and backend integration now covers export package, export request tracking, and parent account deletion. | Legal copy, final legal retention windows, backup deletion propagation, richer CMS review roles, off-box production export delivery, and production audit policy are not complete. Several routes intentionally show blocked states. | Route shell, parent auth/settings/gate, sound controls, child profile UI, onboarding consent, parent trust center, course exploration, market/variant-aware learning player, practice flow, reward surfaces, and first content-admin workflow exist, not product complete. |
| Rust backend | Axum server compiles; `/health`, `/api/health/db`, `/api/pages`, `/api/home/summary`, parent auth/settings endpoints, child profile CRUD endpoints, market/variant-aware course/lesson/activity read endpoints, content-admin course/lesson/activity CRUD endpoints, admin asset list, admin publish check, content version history, learning session/attempt/progress/reward endpoints, and consent/privacy/data deletion endpoints exist; parent accounts persist sound settings (`sound_enabled`, `voice_volume`, `effect_volume`, `auto_play_voice`) and content-admin permission (`is_content_admin`); activity responses include `answer_key` for practice grading and merge selected English-variant overrides; correct attempts persist `activity_mastery` rewards; US/DE/UK child profiles require granted `parental_privacy` consent before learning sessions or attempts can write child learning data; the backend now has a local security baseline for configured CORS, state-changing request Origin checks, process-local API/auth rate limiting, Argon2 password hashes, hashed bearer sessions, and parameterized SQL; config, domain, errors, repositories, routes, services, state, security, and migrations are split into modules; DB connectivity can be checked; `cargo run -- migrate` applies embedded migrations; auth/settings, sound preferences, child CRUD, course API, content admin, progress, rewards, privacy, and security smoke passed against PostgreSQL/router tests. `cargo test --test product_flow_integration -- --nocapture` covers the current backend success and rejection flows, including invalid market/variant rejection and British/American content override behavior; `cargo test --test security_baseline -- --nocapture` covers untrusted Origin rejection, allowed Origin pass-through, and auth rate limiting. | `/api/pages` and `/api/home/summary` still return bootstrap static data through `BootstrapPageRepository`; CMS lacks multi-role review/approval policy, import/export tooling, and full market/variant editing UI. Rate limiting is process-local and must move to an edge/shared store before production scale. | Backend foundations, first CMS workflow, market/variant content selection, and local security baseline exist, not product complete. |
| PostgreSQL | Docker compose defines a PostgreSQL 17 service on assigned host port `37432`; backend has `schema_migrations` plus initial product tables for parent accounts, children, courses, lessons, activities, sessions, attempts, progress, rewards, assets, consents, audit logs, and content version records; generated image asset rows are seeded with provenance metadata; courses, lessons, and activities now carry market-region and English-variant availability arrays, and activities can store JSONB variant overrides. | No CMS import/export flow and no local/staging migration promotion flow. Core CI now runs migration verification against PostgreSQL, but production migration promotion is still not implemented. | Initial schema, first asset seed, CMS version history, content-selection metadata, and CI migration verification exist. |
| Content | Six draft core courses are seeded into PostgreSQL by `0004_seed_core_courses`: animals, colors, numbers, family, food, and daily greetings. The seed currently creates 6 courses, 6 lessons, and 29 activities/questions; course/lesson/activity APIs read them from DB; `0009_market_variant_content_selection.sql` adds selection metadata, restricts one greeting activity outside DE, and adds American `color` / British `colour` copy overrides for the blue word card. | Courses are still `draft`; audio IDs are `null`; no CMS publish workflow blocks incomplete lessons yet. | Draft DB seed and first market/variant selection rules exist. |
| Assets | The first reviewed generated image batch exists under `assets/images`: color course cover, apple word card, activity mastery badge, and two guide expressions; 11 real procedural WAV UI/learning sound effects exist under `assets/audio/ui`; image and audio assets are registered in `assets/asset_manifest.json`; the five reviewed images are also seeded into the DB `assets` table with model/date/use/license/reviewer metadata; all assets pass the content/asset checker. | No teaching voice/audio clips, no video assets, and no final store/legal asset provenance review beyond the internal manifest and DB metadata. | Partial. |
| Tools / artifacts | RunComfy Docker helper, artifact builder scripts, full delivery verification script, backend content publish checker, Playwright E2E script, browser visual smoke script, content/asset checker, secret scan, GitHub Actions core verification workflow, and first HyperFrames video-template project exist; generated docx/xlsx artifacts and a draft MP4 template reel exist. | No production deployment/CD pipeline, no automated mobile SDK/permission scanner, and no final production video render pipeline. | Partial. |
| Documentation | Page-level docs, PRD, no-mock completion standard, delivery verification checklist, art bible, port plan, market/legal docs, child privacy data map, COPPA/GDPR/UK checklist, SDK inventory, App Store/Google Play metadata draft, DE/UK/US listing copy draft, security baseline, staging/production deployment plan, operations/support plan, and backlog exist. | Docs describe a launch-grade product that is not implemented yet; docs must not be treated as working product evidence. Store metadata and listing copy are drafts and still require legal review plus final production SDK/permission/hosting data. Deployment and operations are planned but not implemented. | Planning complete enough to implement. |
| Ports | Repo config uses assigned ports: frontend `37173`, API `37200`, PostgreSQL `37432`; Vite uses `strictPort: true`; Docker host port is migrated. | Any future service work must still read the workspace port registry and run the port policy check before starting services. | Port migration appears complete. |

## Fixed / Demo / Planned Inventory

These items must not be marked complete until replaced with real data flow or explicit blocked states:

| File | Finding | Required Follow-Up |
| --- | --- | --- |
| `backend/src/repositories/pages.rs` | `BootstrapPageRepository` returns static page/home payloads. | Replace with PostgreSQL-backed repositories as product APIs are implemented; keep this classified as bootstrap data, not completion evidence. |
| `backend/src/repositories/auth.rs` | Parent account register/login/me/settings/logout uses PostgreSQL, Argon2 password hashes, hashed bearer sessions, and revocation; integration tests cover duplicate account, bad login, successful login/me, settings update, logout, and revoked token. | Add password reset entrypoint and email verification before production. |
| `backend/src/security.rs` | Adds configured CORS allow-list, state-changing request Origin guard, in-memory API/auth rate limiting, and hashed rate-limit identities; `backend/tests/security_baseline.rs` covers untrusted Origin rejection, allowed Origin requests, and `429 rate_limited`. | Replace process-local rate limiting with edge/shared-store enforcement before production and set exact production `ALLOWED_ORIGINS`. |
| `backend/src/repositories/children.rs` | Child profile create/list/get/update/delete uses PostgreSQL and parent ownership filters; integration tests cover create/list and cross-parent rejection. | Add frontend wiring and broader update/delete route tests. |
| `backend/src/repositories/courses.rs` | Course, lesson, activity, and activity answer key read APIs use PostgreSQL and seeded JSONB content; optional `market_region` and `english_variant` filters are validated and applied across course, lesson, and activity availability; selected activity JSONB variant overrides are merged into prompt/content/answer-key responses; integration tests cover seeded course discovery, missing-course rejection, invalid filters, regional activity filtering, and British/American override behavior. | Add published-only child visibility, age-band filters, and richer admin content mutation for selection metadata. |
| `backend/src/repositories/progress.rs` | Learning sessions, attempts, progress records, and rewards use PostgreSQL and enforce parent-child ownership; US/DE/UK children require granted `parental_privacy` consent before learning writes; correct attempts award idempotent `activity_mastery` rewards; integration tests cover consent-required rejection, session start, attempt write, progress update, reward award/list, completion, cross-parent rejection, and invalid score rejection. | Add audio-play events and richer reward rules. |
| `backend/src/repositories/privacy.rs` / `backend/src/repositories/retention.rs` | Consent grant/list/revoke, data export package generation, DB-backed data export request status tracking, child data deletion, parent account deletion, and local DB retention cleanup use PostgreSQL with parent ownership checks and audit logs; integration tests cover grant/list/revoke/export/delete plus missing child, cross-parent export, cross-parent delete, deleted-token rejection, export request list/status, expired session cleanup, revoked consent evidence minimization, and old detached audit log deletion. | Add production retention scheduling, final legal retention windows, backup deletion propagation, off-box production export delivery, support visibility, and reviewed public copy. |
| `backend/src/repositories/health.rs` | Health checks use PostgreSQL connectivity. | Keep for readiness diagnostics and cover in automated smoke/integration checks. |
| `frontend/src/App.tsx` | Route registry is real application shell structure; API failure now shows unavailable states rather than falling back to in-memory page payloads. | Wire learning, practice, and remaining parent/privacy flows to real APIs. |
| `frontend/src/App.tsx` | Topbar, rail, and primary actions navigate with the History API. | Continue replacing blocked states as each feature flow is implemented. |
| `frontend/src/App.tsx` | Parent sign-up/login/session restore/settings/logout is wired to the Rust auth API with local token storage, loading states, and mapped backend errors. | Move auth into shared client modules once the route grows further. |
| `frontend/src/App.tsx` | Child profile list/create/edit/delete is wired to the Rust child API with bearer auth and real option values; delete is parent-gated. | Add reviewed legal copy around destructive child operations. |
| `frontend/src/App.tsx` | Parent center shows real per-child progress/reward summaries and privacy controls for consent revoke, downloadable data export, child data deletion, and parent account deletion; adult/destructive actions are parent-gated with password verification. | Add reviewed legal copy, production export delivery/status detail, and final deletion help copy. |
| `frontend/src/App.tsx` | Onboarding records market/age/variant choices and can write parent privacy consent to PostgreSQL through the privacy API. | Replace summary guidance with reviewed legal copy after P0-LEGAL work. |
| `frontend/src/App.tsx` | Course exploration reads courses, lessons, objectives, and activities from the Rust API. | Connect selected lessons to the learning player route. |
| `frontend/src/App.tsx` | Learning player starts sessions, records attempts, refreshes child progress/rewards, renders reward pills, and completes sessions against the Rust learning APIs. | Replace manual correct/needs-practice controls with real activity interactions and audio assets. |
| `frontend/src/App.tsx` | Practice game renders single-choice activities, grades choices with backend `answer_key`, records attempts, refreshes progress/rewards, and shows the persisted reward count and `Activity Mastery` pill. | Add richer game feedback, animation, and audio once assets exist. |
| `content/courses/animal-english-words.json` | Source draft course artifact remains valid JSON; lesson audio IDs are `null`. | Expand source content files or importer after DB seed format stabilizes. |
| `backend/migrations/0004_seed_core_courses.sql` | Seeds the first batch of real draft course data into PostgreSQL: 6 courses, 6 lessons, and 29 activities/questions across animals, colors, numbers, family, food, and daily greetings. | Add audio/image asset references, content QA review metadata, and publish gating before marking courses production-ready. |
| `backend/migrations/0006_seed_generated_image_assets.sql` | Seeds the first reviewed image asset batch into PostgreSQL, connects the color course cover to `color-english-words`, and links the apple word card to the food course apple activity content. | Add more course-specific image/audio references before publishing the draft course set. |
| `backend/migrations/0007_generated_image_asset_metadata.sql` | Adds model, generation date, license status, reviewer, and intended use metadata to the generated image asset rows. | Replace internal review text with final legal/store provenance records before production release. |
| `backend/migrations/0008_content_admin_workflow.sql` | Adds `parent_accounts.is_content_admin` and `content_versions` so content-admin actions require an admin-gated parent session and write version snapshots. | Add multi-role review/approval workflow and production retention policy for content version history. |
| `backend/migrations/0009_market_variant_content_selection.sql` | Adds `market_regions` and `english_variants` arrays to courses, lessons, and activities; adds activity `variant_overrides`; adds GIN indexes and constraints; seeds the first DE availability difference and American/British spelling override. | Add CMS editing controls, import/export coverage, and age-band selection metadata. |
| `assets/asset_manifest.json` | Five generated image entries and 11 UI/learning audio entries have materialized paths, approved status, source, prompt summaries, and image metadata. | Add teaching voice clips, generated video entries, and final review metadata. |
| `assets/images/course-covers/color-english-words-cover.png` | Reviewed generated cover art for the color words course, copied to both workspace assets and frontend public assets. | Add remaining course covers before publishing the full first course batch. |
| `assets/images/word-cards/apple-word-card.png` | Reviewed generated apple card art linked from the food course apple activity content. | Generate word-card art for the rest of the word-card activities. |
| `assets/images/badges/activity-mastery-badge.png` | Reviewed generated reward badge art for activity mastery. | Wire badge art into reward surfaces once the frontend reward design is finalized. |
| `assets/images/characters/guide-happy.png` and `assets/images/characters/guide-thinking.png` | Reviewed generated guide character expressions for encouragement and thinking states. | Expand expression set and animation-ready character references. |
| `assets/video/generated/seedance-guide-color-intro.request.json` | Records the blocked Seedance 2.0 Pro request, intended output path, reference assets, and RunComfy authentication blocker. | Submit after RunComfy CLI authentication or `RUNCOMFY_TOKEN` is available. |
| `video/futurelight-templates` | HyperFrames project with four renderable compositions: course intro, word review, parent weekly report, and social short. `npm.cmd run check` passes lint/validate/inspect with only the browser AudioContext autoplay warning, and a draft MP4 render exists at `video/futurelight-templates/renders/futurelight-template-reel.mp4`. | Replace template copy/data with reviewed production scripts and regenerate final quality renders. |
| `assets/audio/ui/*.wav` | First batch of real UI/learning effect files exists for click, toggle, soft error, step complete, unit complete, answer correct/wrong, reward star/badge, mission complete, and page success. | Add final sound-design review and compressed mobile/web variants before production. |
| `tools/generate-ui-audio.mjs` | Reproducibly generates the current WAV effect files through procedural synthesis. | Replace or tune with final sound design before production if needed. |
| `backend/migrations/0005_parent_sound_preferences.sql` | Adds DB-backed parent sound preferences for sound enabled, teaching voice volume, UI effect volume, and voice autoplay. | Add account-level default presets and future child-specific overrides if needed. |
| `frontend/src/App.tsx` | SoundProvider loads real WAV assets, plays navigation/practice/learning/settings cues, and parent settings save sound preferences through the real auth settings API. | Add teaching voice playback once lesson voice assets exist. |
| `docs/art-bible.md` | Defines visual direction for FutureLight, including character proportions, palette, illustration style, prohibited content, age-band differences, card/cover layouts, manifest rules, prompt template, and review checklist. | Keep using it as the gate before `P1-ASSET-005` animation generation and later asset batches. |
| `docs/child-privacy-data-map.md` | Maps current parent, child, learning, consent, audit, localStorage, export package, account deletion, and future voice/SDK data against implemented APIs and PostgreSQL storage. | Use this as source input for public privacy policy, direct parent notice, retention jobs, and P0-LEGAL-002 checklist. |
| `docs/coppa-gdpr-uk-childrens-code-checklist.md` | Tracks US COPPA, DE GDPR Article 8 default-16, and UK high-privacy rules as engineering gates; documents the backend consent gate, export package, parent account deletion, and local security baseline. | Convert checklist into public notices, retention jobs, production export delivery/status policy, and SDK inventory gates. |
| `docs/sdk-inventory.md` | Lists current runtime/build dependencies, confirms no third-party child-flow SDK is present, and blocks ads, analytics, crash reporting, payments, speech/AI, push, social, location, camera, and microphone SDKs until reviewed. | Add production infrastructure/vendor entries, app-store Data Safety/App Privacy labels, and automated SDK/permission scanning. |
| `docs/store-metadata-draft.md` | Drafts App Store Connect and Google Play Console metadata, including product page fields, Kids/Families positioning, age rating answers, App Privacy labels, Google Data Safety answers, review notes, reviewer account placeholders, and release blockers. | Finalize only after privacy policy URL, production hosting/SDK inventory, native permissions, screenshots, preview assets, support/deletion page, and legal review are complete. |
| `docs/store-listing-copy-de-uk-us.md` | Drafts Germany, UK, and US store listing copy, including localized subtitles, promotional text, short descriptions, long descriptions, keyword themes, privacy/trust positioning, screenshot overlay lines, screenshot source requirements, Apple screenshot notes, Google Play icon/feature graphic/screenshot requirements, and feature graphic direction. | Capture final native screenshots, create reviewed feature graphics, approve Kids/Families positioning, and localize privacy/support URLs before submission. |
| `docs/security-baseline.md` | Documents implemented local backend security controls, limits, and production follow-ups for CORS, Origin guard, rate limiting, password/token handling, SQL query safety, and validation. | Replace process-local limits with production edge/shared-store controls and complete password-reset/legal/retention flows. |
| `docs/deployment-plan.md` | Defines the planned staging/production topology, runtime configuration, release flow, backup/restore expectations, monitoring/alerts, rollback approach, core CI status, and launch blockers. | Implement production deployment/CD, choose hosting providers/domains/certificates/object storage, and run staging rehearsals. |
| `docs/operations-support-plan.md` | Defines planned support channels, privacy/support request triage, data deletion/export handling, refunds, content error reports, store review rejection handling, escalation paths, response macros, and operations metrics. | Configure support mailbox/tracker, add production export delivery/status tracking, write public FAQ/macros, and connect support workflows to product/admin tooling. |
| `docs/交付驗證命令清單.md` | Defines the required delivery commands and points to `tools/verify-delivery.ps1`, `tools/run-playwright-e2e.ps1`, `tools/run-browser-visual-smoke.ps1`, `tools/check-content-assets.mjs`, and `tools/check-secrets.ps1`. | Keep expanding route assertions as new flows become production-ready. |
| `tools/run-playwright-e2e.ps1` and `tools/playwright-e2e-flow.js` | Runs a reusable Playwright CLI E2E through signup, login, child profile creation, parental privacy consent, learning, practice, parent center, and course library flows on assigned ports; wrapper starts only missing local services, requires the frontend URL to be reachable on `127.0.0.1`, and cleans up `playwright-e2e@futurelight.test`. | Add richer failure screenshots/traces. |
| `tools/run-browser-visual-smoke.ps1` and `tools/playwright-visual-smoke.js` | Runs desktop and mobile Playwright CLI visual smoke for home, courses, learn, practice, and parent routes; checks horizontal overflow, visible buttons, button text fit, broken images, and loaded course detail; saves screenshots to `output/playwright/visual-smoke`. | Add screenshot diff baselines only after the UI becomes visually stable. |
| `tools/check-content-assets.mjs` | Validates course JSON, asset manifest structure, materialized asset paths, and published-course audio requirements. | Extend after content CMS, generated videos, and DB-backed asset inventory land. |
| `tools/check-secrets.ps1` | Scans common API keys, private keys, cloud tokens, payment keys, and high-risk secret assignments while excluding generated/build folders. | Extend with vendor-specific patterns as SDKs are added. |
| `backend/src/content_checker.rs` | Adds `cargo run -- check-content` to block published courses with missing cover, lessons, activities, answer keys, required word-card materials, missing assets, or unapproved assets. | Wire into CMS publish API so publish attempts fail before DB status changes. |
| `backend/src/repositories/content_admin.rs`, `backend/src/services/content_admin.rs`, `backend/src/routes/content_admin.rs` | Adds admin-gated course CRUD, lesson CRUD, activity CRUD, asset listing, publish-check preview, and content version history endpoints. | Add richer editorial roles, imports, review assignments, and production-grade audit views. |
| `frontend/src/App.tsx` `/content` route | Provides first content-admin UI for course/lesson/activity editing, cover and word-card asset selection, publish checks, and version history. | Add reviewed editorial UX, bulk import, diff views, and role management. |
| `tools/verify-delivery.ps1` | Runs port policy, `git diff --check`, migrations, Rust fmt/clippy/test, backend content publish check, frontend build/lint, Playwright E2E, browser visual smoke, content/asset checker, and secret scan. | Wire into CI once CI exists. |
| `tools/build_project_artifacts.mjs` | Artifact inventory explicitly reports one draft course and planned audio IDs. | Keep as planning evidence only; do not use as completion evidence. |

## Immediate Execution Order

1. P1-ASSET-005 remains blocked until RunComfy CLI authentication or `RUNCOMFY_TOKEN` is available.
2. Continue with release gate review and implementation blockers after P2-OPS-002.

## Audit Conclusion

The repository is buildable and port-safe, and it now has a verified initial PostgreSQL schema plus real parent auth/settings/gate/session, DB-backed sound preferences, child profile, market/variant-aware course reads, first content-admin workflow, learning progress/reward, consent/privacy APIs with downloadable export package, export request status tracking, parent account deletion, a local DB retention cleanup command, six draft core course seeds, backend content publish rules, local backend security baseline, staging/production deployment plan, operations/support plan, GitHub Actions core verification workflow, real UI audio files with runtime SoundProvider playback, the first reviewed generated image batch, first HyperFrames video-template reel, art bible, child privacy data map, COPPA/GDPR/UK checklist gates, SDK inventory, App Store/Google Play metadata draft, DE/UK/US listing copy draft, Playwright E2E, browser visual smoke, delivery verification checklist/scripts, and backend integration coverage for the current product flow. It still remains in early implementation: production retention scheduling, backup deletion propagation, legal copy, broader reviewed images/video assets, production CMS roles/imports, final store assets, production deployment/CD/hosting implementation, support tooling, off-box production export delivery, and store readiness are incomplete. Seedance generation remains blocked by missing RunComfy authentication.
